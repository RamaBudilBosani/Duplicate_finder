arr = [1,2,3,4,1]
has_duplicate=False           #stores false initially
for i in range(0,len(arr)):
    for j in range(i+1,len(arr)):
       if arr[i]==arr[j]:
           has_duplicate=True   #if it finds duplicate then it becomes true
           break
print(has_duplicate)
       
             
           
